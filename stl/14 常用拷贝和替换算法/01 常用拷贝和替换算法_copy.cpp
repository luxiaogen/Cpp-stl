#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
// 常用拷贝和替换算法 copy
/*void myPrint(int val)
{
  cout << val << " ";
}
void test01()
{
  vector<int> v1;
  for (int i = 0; i < 10; i++)
    v1.push_back(i + 1);
  vector<int> v2;
  // 提前分配空间，不然会报错
  v2.resize(v1.size());
  copy(v1.begin(), v1.end(), v2.begin());

  for_each(v2.begin(), v2.end(), myPrint);
  cout << endl;
}*/
int main1()
{
  // test01();
  return 0;
}