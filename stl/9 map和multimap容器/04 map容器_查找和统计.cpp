#include <map>
#include <iostream>
using namespace std;

/*void test01()
{
  map<int,int> m1;
  m1.insert(pair<int, int>(1, 10));
  m1.insert(pair<int, int>(4, 40));
  m1.insert(pair<int, int>(2, 20));
  m1.insert(pair<int, int>(3, 30));

  map<int,int>::iterator pos = m1.find(4);
  if (pos != m1.end())
    cout << "找到了 key = " << (*pos).first << " value = " << pos->second << endl;
  else
    cout << "没找到" << endl;

  // 统计
  // map不允许插入重复 key元素，count统计而言，结果要么是0，要么是1
  // multimap的count统计可能大于1
  int ret = m1.count(3);
  cout << ret << endl;
}*/
int main4()
{
  // test01();
  return 0;
}