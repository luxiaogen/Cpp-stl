#include <map>
#include <iostream>
using namespace std;
/*class MyCompare
{
public:
  bool operator()(int v1, int v2)
  {
    return v1 > v2;
  }
};
void printMap(map<int,int,MyCompare> &m)
{
  for (map<int,int,MyCompare>::iterator it = m.begin(); it != m.end(); it ++)
  {
    cout << "key = " << (*it).first << ",value = " << it->second << endl;
  }
  cout << endl;
}

void test01()
{
  // 默认key从小到大排序
  // 利用仿函数实现从大到小排序
  map<int,int,MyCompare> m = {
          {1,20},{2,10},{5,10},{3,37},{10,188}
  };

  // 遍历
  printMap(m);

}*/
int main5()
{
  // test01();
  return 0;
}