#include <iostream>
#include <vector>
using namespace std;
// vector容器-数据存取

/* void test01()
{
  vector<int> v;
  for (int i = 0; i < 10; i++)
    v.push_back(i + 1);
  // 使用[]访问数组
  for (int i = 0; i < v.size(); i ++)
    cout << v[i] << " ";
  cout << endl;
  // 使用at访问数组
  for (int i = 0;i < v.size(); i ++)
    cout << v.at(i) << " ";
  cout << endl;

  // vector的第一个元素
  cout << "首元素为:" << v.front() << endl;
  // vector的尾元素
  cout << "最后一个元素为：" << v.back() << endl;


} */
int main5()
{
  // test01();
  return 0;
}