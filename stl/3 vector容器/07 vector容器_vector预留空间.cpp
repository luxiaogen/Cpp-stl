#include <iostream>
#include <vector>
using namespace std;

/*void test01()
{
  vector<int> v;
  int num = 0; // 统计开辟次数
  int *p = NULL;
  // 利用reserve预留空间
  v.reserve(100000);
  for (int i = 0; i < 100000; i++)
  {
    v.push_back(i);
    if (p != &v[0])
    {
      p = &v[0];
      num++;
    }
  }
  cout << "num = " << num << endl;
}*/

int main7()
{
  // test01();
  return EXIT_SUCCESS;
}