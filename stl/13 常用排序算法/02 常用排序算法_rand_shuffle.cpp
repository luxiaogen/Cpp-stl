#include <iostream>
#include <vector>
#include <algorithm>
#include <ctime>
using namespace std;
/*class myPrint
{
public:
  void operator()(int val)
  {
    cout << val << " ";
  }
};*/
// 常用排序算法 _rand_shuffle() 方法
/*void test01()
{
  vector<int> v;
  for (int i = 0; i < 10; i++)
    v.push_back(i + 1);
  // 打乱前
  cout << "打乱前" << endl;
  for_each(v.begin(), v.end(), myPrint());
  cout << endl;

  // 打乱顺序
  cout << "打乱后" << endl;
  random_shuffle(v.begin(), v.end());
  for_each(v.begin(), v.end(), myPrint());
}*/

int main2()
{
  // test01();
  return EXIT_SUCCESS;
}