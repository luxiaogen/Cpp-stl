#include <iostream>
#include <set>
using namespace std;
/*void printSet(set<int> &s)
{
  for (set<int>::iterator it = s.begin(); it != s.end(); it ++)
    cout << *it << " ";
  cout << endl;
}
// set容器构造和赋值
void test01()
{
  // 默认无参构造
  set<int> s = {10, 50, 30, 40, 10, 20};
  // 插入元素 只有insert一种方式
  // s.insert(10);
  // s.insert(20);
  // s.insert(30);
  // s.insert(40);

  // 遍历容器
  // set容器特点：所有元素在被插入的时候会被排序
  // set容器不能有重复的值
  printSet(s);

  // 拷贝构造
  set<int> s2(s);
  printSet(s2);

  set<int> s3;
  s3 = s;
  printSet(s3);
}*/
int main1()
{
  // test01();
  return 0;
}