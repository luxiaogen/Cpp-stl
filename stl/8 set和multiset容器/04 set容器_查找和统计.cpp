#include <iostream>
#include <set>
using namespace std;

// 查找
/*void test01()
{
  set<int> s = {20, 10, 30, 50};
  // 迭代器会指向s的最后一个元素的后一个
  set<int>::iterator pos = s.find(50);
  if (pos != s.end())
    cout << "找到元素:" << *pos << endl;
  else
    cout << "未找到元素" << endl;
}

// 统计
void test02()
{
  set<int> s = {20, 10, 30, 50, 30, 30, 30};
  // 对于set而言 count的结果要么是0 要么是1
  int num = s.count(30);
  cout << "num = " << num << endl;
}*/
int main4()
{
  // test01();
  // test02();
  return EXIT_SUCCESS;
}