#include <iostream>
#include <set>
using namespace std;
/*void printSet(set<int> &s)
{
  for (set<int>::iterator it = s.begin(); it != s.end(); it ++)
    cout << *it << " ";
  cout << endl;
}
// 插入删除操作
void test01()
{
  set<int> s;
  // 插入数据
  s.insert(10);
  s.insert(20);
  s.insert(30);
  s.insert(40);

  // 遍历
  // 10 20 30 40
  printSet(s);

  // 删除
  // 20 30 40
  s.erase(s.begin());
  printSet(s);
  s.erase(20);
  printSet(s); // 30 40

  // 清空
  s.erase(s.begin(), s.end());
  s.clear();
}*/
int main3()
{

  // test01();
  return 0;
}