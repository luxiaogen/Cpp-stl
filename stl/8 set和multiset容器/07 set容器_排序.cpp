#include <iostream>
#include <set>
using namespace std;
/*void printSet(set<int> &s)
{
  for (set<int>::iterator it = s.begin(); it != s.end(); it ++)
    cout << *it << " ";
  cout << endl;
}
// 使用仿函数
class MyCompare
{
public:
  bool operator()(int v1, int v2)
  {
    return v1 > v2;
  }
};
// set容器排序
void test01()
{
  // 默认升序
  set<int> s1 = {60, 20, 30, 40, 50};
  printSet(s1);

  // 在创建就需要告诉set是按哪种排序
  set<int, MyCompare> s2 = {19, 20, 40, 20, 10};
  for (set<int,MyCompare>::iterator it = s2.begin(); it != s2.end(); it ++ )
    cout << *it << " ";
  cout << endl;
}*/
int main7()
{
  // test01();
  return EXIT_SUCCESS;
}