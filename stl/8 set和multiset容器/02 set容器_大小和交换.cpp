#include <iostream>
#include <set>
using namespace std;
/*void printSet(set<int> &s)
{
  for (set<int>::iterator it = s.begin(); it != s.end(); it ++)
    cout << *it << " ";
  cout << endl;
}
// 大小
void test01()
{
  set<int> s = {10, 30, 40, 20};
  // 判断set是否为空
  if (s.empty())
  {
    cout << "set为空" << endl;
  }
  else
  {
    cout << "set不为空" << endl;
    cout << "s.size() = " << s.size() << endl;
  }
}

// 交换
void test02()
{
  set<int> s1 = {10, 30, 40, 20};
  set<int> s2 = {100, 300, 400, 200};
  cout << "交换前" << endl;
  printSet(s1);
  printSet(s2);
  s1.swap(s2);
  cout << "交换后" << endl;
  printSet(s1);
  printSet(s2);
}*/
int main2()
{
  // test01();
  // test02();
  return EXIT_SUCCESS;
}