#include <iostream>
#include <deque>

using namespace std;
/*void test01()
{
  deque<int> d;
  d.push_back(1);
  d.push_back(0);
  d.push_front(10);
  d.push_front(100);
  d.push_front(1000);

  // 使用[]访问deque
  for (int i = 0; i < d.size(); i++)
    cout << d[i] << " ";
  cout << endl;

  for (int i = 0; i < d.size(); i++)
    cout << d.at(i) << " ";
  cout << endl;

  cout << "首元素：" << d.front() << endl;
  cout << "尾元素：" << d.back() << endl;
}*/

int main5()
{
  // test01();
  return 0;
}